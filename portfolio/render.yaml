services:
  - type: web
    name: portfolio
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn portfolio.wsgi:application
    envVars:
      - key: DJANGO_SECRET_KEY
        value: django-insecure-fd%$-tzt%*t)d_+kefm_%@znv0eu2gys=r*#batlrarzv_rs)
      - key: DEBUG
        value: False
      - key: ALLOWED_HOSTS
        value: portfolio.onrender.com
